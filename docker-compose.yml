services:
  db:
    image: postgres:16
    environment:
      POSTGRES_USER: nfl
      POSTGRES_PASSWORD: nfl
      POSTGRES_DB: nflstats
    ports:
      - "5432:5432"
    volumes:
      - nfl_pg:/var/lib/postgresql/data

volumes:
  nfl_pg:
